rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Internal dashboard — all collections open for read/write.
    // Lock down with proper auth rules once user login is implemented.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
